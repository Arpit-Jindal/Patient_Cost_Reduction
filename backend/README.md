### Backend Patient Cost Reduction

Technologies Used: Python, Flask

Run `python install -r requirements.txt` to install the requirements.
Run `python app.py` to run the server on http://127.0.0.1:5000/
